# --- Microsoft Entra app registration ---
# From Azure Portal / entra.microsoft.com -> App registrations -> your app
MS_CLIENT_ID=

# Personal/school test tenant: use "consumers" for a personal MSA,
# or your specific tenant ID/domain for a school or org account.
# When this moves to MTN, this becomes MTN's tenant ID -- nothing else changes.
MS_TENANT_ID=common

# --- Scope of this PoC ---
# Dedicated test folder inside the mailbox we're reading from.
# NEVER point this at "Inbox" during development.
MAIL_TEST_FOLDER_NAME=AI-Demo

# Delegated Graph scopes this app requests. Space-separated.
GRAPH_SCOPES=Mail.ReadWrite Mail.Read User.Read

# --- Classification (Milestone 2) ---
# Get a free key at https://aistudio.google.com/apikey -- no card required.
GEMINI_API_KEY=

CLASSIFY_MODEL=gemini-2.5-flash

# Below this confidence (0-1), route to "needs review" instead of
# auto-filing, regardless of category.
CONFIDENCE_THRESHOLD=0.7